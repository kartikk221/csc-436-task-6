import Container from './Container';

const Footer = () => {
    return (
        <Container className="rounded-t-lg z-50 bg-blue-400 text-white font-bold px-8 fixed left-0 right-0 bottom-0 w-screen">
            Discover some of the biggest classics in literature!
        </Container>
    );
};

export default Footer;
